
- increase tests
- EnterInput.val isn't applied, I think
- EnterInput test, have a chain of open an close to show state is tidied correctly
- this might be child components being ready before parent.  [Vue warn]: injection "shopping" not found.
- engineer thing to that actual network code is masked in story tests
- pour les mot variable privé, traduire en francias 
- Update static route to include new ids
- check ARIA values 
- check for alternatives for deps with non-standard packing
- swap js-json-object with typedjson
- check that values in AList are populated correctly
- finish localisation / convert to import libs
- update localisation to a better string storage 
- work out what MSG_DESTINATION should be
- workout if can installworker filelist to a manifest?
- postMessage targets ~ set target address when known
- StateSyncing test cover full state map. when threads work
- check other means to engineer motionstream
- think about best name for ShopState
- may be need to re-add STORE.getters
- look at closure generators to hold my "module variable" static vars safe after compilation/ bundling
- workout if MotionStream->significance and signifcanceAsPercentage should use same thresholds
- edit clean.bash to current dir tree
- add the better validation on transform2list
- bash scripts need to check version of node
- On browser tab maximisation, poll() server to see if still live && accessible
- On list state update, work out if new data is the same as old.   compute hash?
https://www.npmjs.com/package/@vitejs/plugin-vue
https://github.com/orimay/async-ts

set NODE_ENV="TEST" for tests
Yes, the storybook test use storybook/test module that currently I can't install.   Storybook seems to include it even though its doesn't exist in the correct version ~ mono-repo.   #WorksForMe, I hope it works-for-you.

export default defineConfig({
  define: {
    __DEV__: process.env.NODE_ENV !== "production",
  },
});

Then in your code:
ts
if (__DEV__) {
  console.log("Debug info:", something);
}


